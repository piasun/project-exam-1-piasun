import { articleList } from "./listOfArticles.js";

export function onSearch(articles){
    const searchField = document.querySelector("input[type='search']");
    searchField.addEventListener("keyup")
}
